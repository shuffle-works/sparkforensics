## What this changes and why

## Test plan

- [ ] `npm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds
- [ ] If `server/` was touched: `npm test` passes there too
- [ ] Manually verified in the browser (describe what was checked)

## Notes for reviewers

Anything that needs context: new thresholds, detector suppression order
changes, UI copy changes, etc.
