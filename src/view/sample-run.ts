/** Session id the bundled sample run loads under, so the dashboard can tell a
 * newcomer that the board in front of them is the sample, not their own run. */
export const SAMPLE_RUN_ID = 'sample-run';
