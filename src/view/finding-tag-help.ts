import { docAnchorForType } from '@sparkforensics/core/docs-config.ts';

// TAG_HELP lives in core (the comparison verdict reads it on the CLI/MCP path too).
export { TAG_HELP } from '@sparkforensics/core/finding-tag-help.ts';
export { docAnchorForType };
