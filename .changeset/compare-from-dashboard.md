---
"sparkforensics-web": minor
---

Compare with another run from a run's dashboard. The top bar (the More options menu on a phone) opens the two-run compare view with the open run already in Run A, so checking whether a change helped means loading only the new run; the open run is reused, not parsed again, and Back to the run returns to its dashboard.
