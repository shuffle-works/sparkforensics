---
"sparkforensics-web": minor
---

When the verdict lists what this log could not check, it now gathers the settings those lines name into one line of spark-submit `--conf` flags with a Copy settings button, so the next run's log can be fully checked without assembling the flags by hand. Copy next steps includes the same line.
