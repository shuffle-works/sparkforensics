---
"sparkforensics-mcp": patch
"sparkforensics": patch
---

`sparkforensics-mcp --help` now lists every registered tool, generated from the server itself. The History Server error text's "Local-server setup" link opens the getting-started section on local-server mode instead of the tuning reference intro.
