---
"sparkforensics-cli": patch
"sparkforensics-mcp": patch
"sparkforensics-server": patch
---

Package READMEs now show a working install command and one example instead of only linking to the main README.
